/// <reference path="./../../includes.d.ts"/>

namespace com.vmware.library.vcaccafe.workflow.subscription {
export function isBlocking(subscription?: vCACCAFE.WorkflowSubscription): boolean {return subscription.isBlocking();
}
}
